<%if @current_user.role != "owner" %>
    <%= form_with(url: orders_path, method: "post") do %>
        <%= submit_tag :"Place new order" %>
    <% end %>
<% end %>
<%= render partial: "order-section", locals: {
    role: @current_user.role
}%>
